<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.mapper.LibraryMapper">

    <!-- 分页查询图书馆 -->
    <select id="selectPageWithCondition" resultType="com.library.entity.Library">
        SELECT 
            id,
            name,
            address,
            description,
            status,
            create_time,
            update_time 
        FROM library
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY id DESC
        LIMIT #{offset}, #{size}
    </select>

    <!-- 查询总数 -->
    <select id="countWithCondition" resultType="long">
        SELECT COUNT(1) 
        FROM library
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
    </select>

</mapper>